<%= form_for(@temp_name) do |f| %>
  <% if @temp_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temp_name.errors.count, "error") %> prohibited this temp_name from being saved:</h2>

      <ul>
      <% @temp_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_area :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br>
    <%= f.text_area :last_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
